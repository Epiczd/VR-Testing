using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class GunCheck : MonoBehaviour
{
    //Determines if the gun is active or not
    public static bool gunActive;

    //OnStart, the gun is not active
    void Start()
    {
        gunActive = false;
    }

    // If the player is holding the gun, gun active is true
    void OnTriggerEnter(Collider collision)
    {
        if (collision.CompareTag("Gun"))
        {
            gunActive = true;
        }
    }

    // If the player is NOT holding the gun, gun active is false
    void OnTriggerExit(Collider collision)
    {
        if (collision.CompareTag("Gun"))
        {
            gunActive = false;
        }
    }
}
*/
